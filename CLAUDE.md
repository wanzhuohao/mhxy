# CLAUDE.md — 梦幻西游辅助工具

## 项目概述

梦幻西游游戏辅助工具，基于图像识别实现自动化任务（师门、宝图、捉鬼、副本、秘境、押镖、挖图、答题），支持多窗口管理。

## 技术栈

- Python 3
- tkinter（GUI）
- OpenCV（图像识别）
- pyautogui（截屏、鼠标控制）
- win32gui/win32api（Windows 窗口操作）

## 目录结构

```
mhxy/
├── src/
│   ├── mhxy.py        # GUI 主程序
│   └── business.py    # 业务逻辑模块
├── templates/
│   ├── shimen/        # 师门任务模板
│   ├── baotu/         # 宝图任务模板
│   ├── fuben/         # 副本相关模板
│   ├── mijing/        # 秘境相关模板
│   ├── yabiao/        # 押镖相关模板
│   ├── watu/          # 挖图相关模板
│   ├── dati/          # 答题相关模板
│   └── common/        # 通用 UI 元素
├── docs/
│   ├── specs/         # 设计文档
│   ├── plans/         # 实现方案
│   └── temp/          # 临时文件
├── CLAUDE.md
└── progress.md
```

## 运行

```bash
python src/mhxy.py
```

## 功能模块

| 功能 | 说明 |
|------|------|
| 窗口管理 | 启动/排列游戏窗口（2行3列布局） |
| 组队 | 自动邀请好友组队 |
| 师门 | 模板匹配 + 自动点击（点击2次后等待完成） |
| 宝图 | 模板匹配 + 自动点击（活动→宝图→听听无妨→等待完成） |
| 捉鬼 | 颜色检测 + 点击序列 |
| 副本/秘境/押镖/挖图/答题 | 模板匹配 + 自动点击 |
| 一条龙 | 按顺序执行押镖→秘境 |
| 抓点 | 全屏透明层，获取坐标和 RGB 颜色 |

## 开发规范

- 模板预加载到 `TPL` 字典，避免重复读磁盘
- 匹配阈值默认 0.8，可按模板调整
- 点击添加随机偏移避免检测
- 所有任务支持 `stop_xxx` 全局标志中断
- 快捷键 F12 停止所有任务
- **改完代码重新测试时，必须先杀掉之前的进程**：`taskkill //F //PID <pid>` 或 `taskkill //F //IM python.exe`
